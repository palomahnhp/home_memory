<h2><%= @user.full_name %>  </h2>

<div class="row" >
  <div class="small-12 columns" >

   <%= render 'shared/appointments', resource: :history, kind: 'pending', user: @user %>

   <%= render 'shared/histories', user: @user  %>

   <br>
   <%= link_to t('actions.back'), users_path %>
  </div>
</div>

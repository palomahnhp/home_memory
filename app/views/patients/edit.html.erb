<div class="row">
  <div class="row">
    <h1><%= t('patients.edit.title')  %></h1>

  <%= render 'form', patient: @patient %>

  <%= link_to t('actions.show'), @patient %> |
  <%= link_to t('actions.back'), patients_path %>
  </div>
</div>
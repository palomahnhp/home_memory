<div class="row">
  <div class="row">
    <h1 class="page-title"><%= t('users.edit.title')  %></h1>

  <%= render 'form', user: @user %>

  <%= link_to t('actions.show'), @user %> |
  <%= link_to t('actions.back'), users_path %>
  </div>
</div>